# StarRocks Database Configuration
SR_HOST=localhost
SR_PORT=9030
SR_USER=root
SR_PASSWORD=
SR_DATABASE=information_schema

# HTTP Server Configuration
PORT=3000

# Central API Server Configuration
# API_HOST: 网络绑定地址
#   - 0.0.0.0: 监听所有网络接口（允许外部访问）
#   - 127.0.0.1 或 localhost: 仅本机访问
#   - 具体IP: 绑定到特定网络接口
API_HOST=0.0.0.0
API_PORT=3002

# Security - API Key for authentication (optional but recommended)
# Generate a secure random key: openssl rand -hex 32
API_KEY=demo-key

# CORS - Comma-separated list of allowed origins
# Use '*' to allow all origins (not recommended for production)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080

# LLM API Keys (for intelligent analysis)
DEEPSEEK_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=

# Remote Agent Configuration (for multi-tenant mode)
# Path to tenants configuration file
TENANTS_CONFIG=./tenants-config.json

# Local Agent Configuration (when running as agent)
AGENT_PORT=8081
AGENT_TOKEN=

# Prometheus Configuration (optional, for metrics-based analysis)
PROMETHEUS_PROTOCOL=http
PROMETHEUS_HOST=localhost
PROMETHEUS_PORT=9090
